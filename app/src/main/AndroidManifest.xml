<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vssut">

    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@drawable/medexpress"
        android:label="@string/app_name"
        android:roundIcon="@drawable/medexpress"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".work"
            android:label="@string/title_activity_work"></activity>
        <activity android:name=".regis" />
        <activity android:name=".verify" />
        <activity android:name=".startscreen" />
        <activity android:name=".JoinNow" />
        <activity android:name=".signin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginHelperActivity" />
    </application>

</manifest>